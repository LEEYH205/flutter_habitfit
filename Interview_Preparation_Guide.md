# 🎯 면접 준비 가이드 - HabitFit MVP 프로젝트

## 📋 목차
1. [면접 질문 및 답변](#면접-질문-및-답변)
2. [면접 준비 체크리스트](#면접-준비-체크리스트)
3. [AI/ML/DL 관련 질문](#aimldl-관련-질문)
4. [백엔드 관련 질문](#백엔드-관련-질문)

---

## 🗣️ 면접 질문 및 답변

### 🚀 프로젝트 소개 관련

#### Q1: HabitFit MVP 프로젝트를 간단히 소개해주세요.
**A:** 
```
HabitFit MVP는 AI 기반 운동 자세 분석과 건강 데이터 관리를 결합한 
모바일 애플리케이션입니다. 

주요 특징:
- TensorFlow Lite 기반 MoveNet 모델을 활용한 실시간 포즈 추정
- 스쿼트, 푸시업 등 운동 자세의 정확도 분석 및 횟수 카운팅
- HealthKit 연동을 통한 건강 데이터 수집 및 분석
- Firebase 기반의 실시간 데이터 동기화 및 푸시 알림

기술 스택: Flutter, TensorFlow Lite, Firebase, HealthKit
```

#### Q2: 이 프로젝트에서 가장 도전적이었던 부분은 무엇인가요?
**A:**
```
가장 도전적이었던 부분은 모바일 디바이스에서의 실시간 AI 추론 최적화였습니다.

도전 요소:
1. TensorFlow Lite 모델의 메모리 사용량 최적화
2. 카메라 스트림에서 30fps 이상의 처리 속도 달성
3. 다양한 조명 조건과 카메라 각도에서의 정확도 향상

해결 방법:
- float16 양자화 모델 사용으로 메모리 절약
- 텐서 shape 강제 설정으로 안정성 확보
- 신뢰도 임계값과 상태 머신을 통한 노이즈 제거
```

#### Q3: 프로젝트에서 사용한 AI 모델을 선택한 이유는?
**A:**
```
MoveNet 모델을 선택한 이유:

1. 경량화: 모바일 디바이스에 최적화된 경량 모델
2. 정확도: 17개 키포인트 추정으로 정확한 자세 분석 가능
3. 속도: 실시간 처리에 적합한 추론 속도
4. Google 지원: 지속적인 업데이트와 커뮤니티 지원

특히 singlepose-lightning 모델은 속도와 정확도의 
균형이 잘 맞춰져 있어 모바일 환경에 적합했습니다.
```

---

## ✅ 면접 준비 체크리스트

### 🤖 AI/ML 개발자 체크리스트

#### 기본 개념 이해
- [ ] **머신러닝 기초**
  - [ ] 지도학습 vs 비지도학습 vs 강화학습
  - [ ] 과적합(Overfitting)과 과소적합(Underfitting)
  - [ ] 교차 검증(Cross Validation)의 개념과 방법
  - [ ] 정규화(Regularization) 기법들

- [ ] **딥러닝 기초**
  - [ ] 신경망의 기본 구조와 활성화 함수
  - [ ] 역전파(Backpropagation) 알고리즘
  - [ ] 경사하강법과 최적화 알고리즘
  - [ ] 배치 정규화(Batch Normalization)

#### Computer Vision
- [ ] **이미지 처리 기초**
  - [ ] 컨볼루션 연산과 필터의 역할
  - [ ] 풀링(Pooling)의 종류와 효과
  - [ ] 데이터 증강(Data Augmentation) 기법
  - [ ] 이미지 전처리 방법들

- [ ] **포즈 추정**
  - [ ] 키포인트 기반 포즈 추정 원리
  - [ ] MoveNet, PoseNet 등 주요 모델 비교
  - [ ] 실시간 처리 최적화 기법
  - [ ] 정확도 향상을 위한 후처리 방법

#### 모바일 AI
- [ ] **모델 최적화**
  - [ ] 모델 양자화(Quantization) 기법
  - [ ] 모델 압축(Pruning) 방법
  - [ ] 지식 증류(Knowledge Distillation)
  - [ ] TensorFlow Lite 최적화 기법

- [ ] **성능 최적화**
  - [ ] 메모리 사용량 최적화
  - [ ] 배터리 효율성 향상
  - [ ] 하드웨어 가속 활용
  - [ ] 추론 시간 최소화

### 🔧 백엔드 개발자 체크리스트

#### 클라우드 서비스
- [ ] **Firebase 이해**
  - [ ] Firestore 데이터 모델링
  - [ ] 보안 규칙 작성 및 관리
  - [ ] 실시간 데이터 동기화
  - [ ] 오프라인 지원 구현

- [ ] **푸시 알림**
  - [ ] FCM 서비스 구조 이해
  - [ ] APNS와의 연동 방법
  - [ ] 토큰 관리 및 갱신
  - [ ] 알림 전송률 최적화

#### 데이터베이스
- [ ] **NoSQL 데이터베이스**
  - [ ] 문서 기반 데이터 모델링
  - [ ] 쿼리 최적화 및 인덱싱
  - [ ] 스케일링 전략
  - [ ] 데이터 일관성 보장

- [ ] **데이터 아키텍처**
  - [ ] 시계열 데이터 처리
  - [ ] 실시간 데이터 스트리밍
  - [ ] 데이터 백업 및 복구
  - [ ] 데이터 마이그레이션

#### 보안 및 인증
- [ ] **사용자 인증**
  - [ ] JWT 토큰 기반 인증
  - [ ] OAuth 2.0 구현
  - [ ] 권한 관리 시스템
  - [ ] 세션 관리

- [ ] **데이터 보안**
  - [ ] 데이터 암호화 방법
  - [ ] API 보안 구현
  - [ ] 개인정보보호 규정 준수
  - [ ] 보안 취약점 방지

---

## 🤖 AI/ML/DL 관련 질문

### 🧠 머신러닝 기초

#### Q1: 지도학습과 비지도학습의 차이점을 설명해주세요.
**A:**
```
지도학습(Supervised Learning):
- 레이블이 있는 데이터로 학습
- 입력과 출력의 관계를 학습
- 예시: 이미지 분류, 스팸 메일 감지
- 평가 지표: 정확도, 정밀도, 재현율

비지도학습(Unsupervised Learning):
- 레이블이 없는 데이터로 학습
- 데이터의 숨겨진 패턴을 발견
- 예시: 클러스터링, 차원 축소
- 평가 지표: 클러스터 품질, 재구성 오차
```

#### Q2: 과적합(Overfitting)을 방지하는 방법은?
**A:**
```
1. 데이터 증강(Data Augmentation)
   - 더 많은 훈련 데이터 확보
   - 기존 데이터 변형으로 다양성 증가

2. 정규화(Regularization)
   - L1, L2 정규화 적용
   - Dropout 레이어 사용
   - Early Stopping

3. 모델 복잡도 조절
   - 레이어 수나 뉴런 수 감소
   - 더 간단한 모델 구조 사용

4. 교차 검증(Cross Validation)
   - K-fold 교차 검증으로 일반화 성능 평가
```

#### Q3: 배치 정규화(Batch Normalization)의 효과는?
**A:**
```
배치 정규화의 효과:

1. 학습 안정성 향상
   - 내부 공변량 이동(Internal Covariate Shift) 감소
   - 그래디언트 소실/폭발 문제 완화

2. 학습 속도 향상
   - 더 높은 학습률 사용 가능
   - 수렴 속도 개선

3. 정규화 효과
   - Dropout과 유사한 효과
   - 일반화 성능 향상

4. 초기화에 대한 민감도 감소
   - 가중치 초기화에 덜 민감
```

### 👁️ Computer Vision

#### Q1: 컨볼루션 신경망(CNN)의 핵심 구성 요소는?
**A:**
```
CNN의 핵심 구성 요소:

1. 컨볼루션 레이어(Convolutional Layer)
   - 특징 추출을 위한 필터 적용
   - 가중치 공유로 파라미터 수 감소
   - 공간적 불변성 학습

2. 풀링 레이어(Pooling Layer)
   - Max Pooling: 최대값 선택으로 특징 강조
   - Average Pooling: 평균값으로 노이즈 감소
   - 차원 축소 및 계산량 감소

3. 활성화 함수(Activation Function)
   - ReLU: 비선형성 추가 및 그래디언트 소실 방지
   - Sigmoid/Tanh: 출력 범위 제한

4. 완전연결 레이어(Fully Connected Layer)
   - 최종 분류를 위한 특징 결합
   - 학습 가능한 가중치로 출력 생성
```

#### Q2: 포즈 추정에서 키포인트의 신뢰도(Confidence)는 어떻게 활용하나요?
**A:**
```
키포인트 신뢰도 활용 방법:

1. 노이즈 필터링
   - 낮은 신뢰도(< 0.3) 키포인트 제외
   - 불안정한 추정 결과 방지

2. 가중 평균 계산
   - 높은 신뢰도 키포인트에 더 높은 가중치
   - 정확한 자세 분석을 위한 신뢰성 향상

3. 동적 임계값 설정
   - 환경 조건에 따른 임계값 조정
   - 조명, 각도 등에 따른 적응적 처리

4. 후처리 개선
   - 신뢰도 기반 스무딩 적용
   - 일관성 있는 자세 추적
```

#### Q3: 실시간 비디오 처리에서 성능을 최적화하는 방법은?
**A:**
```
실시간 비디오 처리 최적화:

1. 모델 최적화
   - 경량화된 모델 사용 (MoveNet Lightning)
   - 양자화(Quantization) 적용
   - 모델 압축(Pruning) 기법

2. 프레임 처리 최적화
   - 프레임 스킵: 모든 프레임 처리하지 않음
   - 해상도 조정: 처리할 이미지 크기 최적화
   - 배치 처리: 여러 프레임을 묶어서 처리

3. 하드웨어 가속
   - GPU 활용: 병렬 처리로 속도 향상
   - NPU/DSP: 전용 AI 칩 활용
   - 멀티스레딩: CPU 코어 분산 활용

4. 메모리 관리
   - 텐서 재사용: 불필요한 메모리 할당 방지
   - 가비지 컬렉션 최소화: 메모리 단편화 방지
```

### 📱 모바일 AI

#### Q1: TensorFlow Lite에서 모델을 최적화하는 방법은?
**A:**
```
TensorFlow Lite 모델 최적화:

1. 모델 변환 최적화
   - Post-training quantization 적용
   - float32 → float16/int8 변환
   - 동적 범위 양자화로 정확도 유지

2. 인터프리터 최적화
   - 스레드 수 조정 (threads = 2-4)
   - 메모리 할당 최적화
   - 텐서 shape 강제 설정

3. 입력 전처리 최적화
   - 이미지 크기 조정 (192x192)
   - 데이터 타입 일치 (uint8/int32)
   - 메모리 복사 최소화

4. 추론 파이프라인 최적화
   - 배치 처리 활용
   - 비동기 처리로 UI 블로킹 방지
   - 결과 캐싱 및 재사용
```

#### Q2: 모바일에서 AI 모델의 배터리 효율성을 높이는 방법은?
**A:**
```
배터리 효율성 향상 방법:

1. 모델 경량화
   - 더 작은 모델 사용
   - 양자화로 메모리 접근 횟수 감소
   - 불필요한 연산 제거

2. 스마트 처리
   - 사용자 활동 감지 시에만 활성화
   - 프레임 레이트 동적 조정
   - 백그라운드 처리 최소화

3. 하드웨어 최적화
   - 효율적인 AI 전용 칩 활용
   - CPU 부하 분산
   - 메모리 대역폭 최적화

4. 전력 관리
   - 전력 소모가 높은 연산 제한
   - 적응적 품질 조정
   - 사용자 설정 기반 동적 조정
```

---

## 🔧 백엔드 관련 질문

### ☁️ Firebase & 클라우드

#### Q1: Firestore에서 데이터 모델링 시 고려사항은?
**A:**
```
Firestore 데이터 모델링 고려사항:

1. 문서 구조 설계
   - 중첩된 데이터 vs 별도 컬렉션
   - 읽기/쓰기 패턴에 따른 최적화
   - 문서 크기 제한 (1MB) 고려

2. 쿼리 최적화
   - 복합 인덱스 생성
   - 쿼리 제한사항 이해 (in, array-contains 등)
   - 페이지네이션 구현

3. 비용 최적화
   - 읽기/쓰기 횟수 최소화
   - 오프라인 지원으로 네트워크 요청 감소
   - 적절한 캐싱 전략

4. 확장성 고려
   - 샤딩 전략 수립
   - 실시간 업데이트 최적화
   - 백업 및 복구 전략
```

#### Q2: FCM에서 푸시 알림 전송률을 높이는 방법은?
**A:**
```
FCM 전송률 향상 방법:

1. 토큰 관리
   - 정기적인 토큰 갱신
   - 앱 재설치 시 토큰 업데이트
   - 유효하지 않은 토큰 정리

2. 메시지 최적화
   - 적절한 메시지 크기 (4KB 이하)
   - 사용자 참여도 높은 시간대 전송
   - 개인화된 메시지 내용

3. 플랫폼별 최적화
   - iOS: APNS 설정 최적화
   - Android: 백그라운드 처리 최적화
   - 크로스 플랫폼 호환성 확보

4. 사용자 참여도 관리
   - 알림 권한 요청 타이밍
   - 사용자 선호도 설정
   - A/B 테스트를 통한 최적화
```

#### Q3: 실시간 데이터 동기화에서 발생할 수 있는 문제와 해결방법은?
**A:**
```
실시간 동기화 문제와 해결방법:

1. 네트워크 불안정성
   - 오프라인 지원 구현
   - 재연결 시 자동 동기화
   - 충돌 해결 전략 수립

2. 데이터 일관성
   - 타임스탬프 기반 순서 보장
   - 낙관적 락(Optimistic Locking) 구현
   - 트랜잭션 사용으로 원자성 보장

3. 성능 이슈
   - 필요한 데이터만 구독
   - 디바운싱/스로틀링 적용
   - 페이지네이션으로 대용량 데이터 처리

4. 보안 문제
   - 사용자별 데이터 접근 제한
   - 실시간 보안 규칙 적용
   - 데이터 암호화 적용
```

### 🗄️ 데이터베이스 & 아키텍처

#### Q1: NoSQL 데이터베이스의 장단점은?
**A:**
```
NoSQL 데이터베이스 장단점:

장점:
1. 확장성: 수평적 확장 용이
2. 유연성: 스키마 변경 자유로움
3. 성능: 특정 쿼리에 최적화된 성능
4. 비용: 오픈소스 기반으로 비용 절약

단점:
1. 일관성: ACID 속성 보장 어려움
2. 복잡한 쿼리: JOIN 연산 제한
3. 트랜잭션: 복잡한 트랜잭션 처리 어려움
4. 학습 곡선: 새로운 쿼리 언어 학습 필요

적합한 사용 사례:
- 대용량 데이터 처리
- 실시간 애플리케이션
- 로그 데이터 저장
- IoT 데이터 수집
```

#### Q2: 마이크로서비스 아키텍처에서 데이터 일관성을 어떻게 보장하나요?
**A:**
```
마이크로서비스 데이터 일관성 보장:

1. Saga 패턴
   - 분산 트랜잭션을 여러 로컬 트랜잭션으로 분해
   - 보상 트랜잭션으로 롤백 처리
   - 이벤트 기반 상태 관리

2. 이벤트 소싱
   - 모든 상태 변경을 이벤트로 기록
   - 이벤트 스토어에서 상태 재구성
   - 이벤트 순서 보장

3. CQRS (Command Query Responsibility Segregation)
   - 읽기/쓰기 모델 분리
   - 이벤트 기반 데이터 동기화
   - 최종 일관성(Eventual Consistency) 보장

4. 분산 락
   - Redis, Zookeeper 등을 활용한 락 관리
   - 데드락 방지를 위한 타임아웃 설정
   - 장애 상황에서의 락 해제 처리
```

#### Q3: API 설계 시 보안을 어떻게 구현하나요?
**A:**
```
API 보안 구현 방법:

1. 인증(Authentication)
   - JWT 토큰 기반 인증
   - OAuth 2.0 / OpenID Connect
   - API 키 관리 및 로테이션

2. 권한 부여(Authorization)
   - 역할 기반 접근 제어(RBAC)
   - 리소스별 권한 검증
   - 세분화된 권한 관리

3. 데이터 보호
   - HTTPS/TLS 암호화
   - 민감 데이터 암호화 저장
   - API 요청/응답 로깅

4. 공격 방지
   - Rate Limiting으로 DDoS 방지
   - 입력 검증 및 SQL Injection 방지
   - CORS 설정으로 CSRF 방지
   - API 버전 관리 및 하위 호환성
```

---

## 🎯 면접 준비 팁

### 📝 기술 면접 준비
1. **프로젝트 코드 리뷰**: 자신이 작성한 코드를 다시 한번 검토
2. **기술 스택 심화 학습**: 사용한 기술의 원리와 장단점 파악
3. **성능 최적화 경험**: 실제로 개선한 사례와 수치 준비
4. **문제 해결 과정**: 직면한 문제와 해결 방법을 단계별로 정리

### 💬 행동 면접 준비
1. **STAR 방법**: Situation, Task, Action, Result 구조로 답변
2. **팀워크 경험**: 협업 과정에서의 역할과 기여도
3. **학습 능력**: 새로운 기술 습득 과정과 적용 사례
4. **문제 해결 능력**: 예상치 못한 상황에서의 대처 방법

### 🔍 질문 준비
1. **회사/팀에 대한 질문**: 기술 스택, 프로젝트, 문화 등
2. **성장 기회**: 학습 프로그램, 멘토링, 컨퍼런스 참여 등
3. **기술적 도전**: 앞으로 해결하고 싶은 기술적 과제
4. **경력 계획**: 단기/장기 목표와 회사와의 연관성

---

*이 가이드는 HabitFit MVP 프로젝트를 기반으로 작성되었으며, AI와 백엔드 개발자 면접 준비에 필요한 핵심 내용을 포함하고 있습니다.*
